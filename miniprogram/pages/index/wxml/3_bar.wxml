
<view class="theme-yellow">
    <bar list="{{tabMatrix[0].list}}" bg="bg-yellow" text="text-white" bindclick="clickbar"></bar>

    <bar-date></bar-date>

    <tabbar matrix="{{tabMatrix}}" index="{{tabbarIndex}}" bindclick="clickTabbar"  bg="bg-black" text="text-red"></tabbar>
</view>