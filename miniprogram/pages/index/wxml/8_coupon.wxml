
<!-- 代金券 -->

<view class="margin-top" wx:for="{{couponList}}"  catchtap="clickCoupon" >
    <coupon-node 
        name="{{item.name}}"
        desc="{{item.desc}}"
        startTime="{{item.startTime}}"
        endTime="{{item.endTime}}"
        discount="{{item.discount}}"
    ></coupon-node>
</view>